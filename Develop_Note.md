# 開発ノート (Develop Note) - Project Elucidator

本ドキュメントは、リアルタイム・ハンドトラッキング技術を用いたゲーム（スタンドアロンアプリ）開発における、環境構築、要件定義、および技術スタックの変遷を記録・管理するためのマスターノートである。

最終更新日 : 2026/06/

---

## 1. 開発背景とアーキテクチャの変遷

### 1.1 初期フェーズ
- **構成**: Python (Anaconda環境) ➔ UDP通信 ➔ Unity 
(mediapipeの初期学習の時に使っていた環境で以前開発していたのでそれをそのまま引き継いだ結果こうなった。mediapipeのsolutionsを使っていたため古いpythonでしか動かない)
- **課題**: Anacondaによる環境依存が強く、Windows/Mac間でのマルチプラットフォーム開発において環境複製コストが非常に高かった。

### 1.2 移行フェーズ（ネイティブプラグインの検証）
- **構成**: Unity 6 ➔ MediaPipe Unity Plugin (C# ネイティブインポート)
- **結果**: Unity 6の内部システム（新入力システム：Input Systemパッケージ等）との衝突、ネイティブライブラリのロードエラー、エディタクラッシュの多発により**開発を断念・凍結**。

### 1.3 現行フェーズ（モダンTasks API + 自動制御構成）
- **構成**: **Python 3.12+ (標準venv環境 / Tasks API) ➔ UDP通信 (Port:50001) ➔ Unity 6**
- **採用理由**: 
  - Google公式の最新仕様である『MediaPipe Tasks API』へ移行したことで、Pythonのバージョンに縛られず、最新のPython 3.12〜3.13環境、およびWindows/Mac両OSでの安定動作を確保。
  - Anacondaを完全に排除し、Python標準の仮想環境（`venv`）に一本化したことで、MacでもWindowsでも全く同じ軽量な環境を即座に構築可能。
  - Unity側からC#の `Process` クラスを用いて、再生ボタンに完全連動して裏のPythonプロセスを自動起動・自動終了（ゾンビ化防止）させるラッパーを実装。開発効率を劇的に向上させた。

---

## 2. 現在の環境構成・バージョン情報

システムを安定動作させるための基本構成スペック：

| 構成要素 | バージョン / 指定スペック | 備考 |
| :--- | :--- | :--- |
| **Unity Editor** | `6000.3.9f1` (Unity 6) | Universal Render Pipeline (URP) 構成 |
| **Python** | `3.12` | `python-tracking`直下の`mise.toml`での導入 |
| **仮想環境** | Python標準 `venv` | プロジェクト内の `/python-tracking/venv/` に配置 |
| **MediaPipe** | `最新の安定版 (Tasks API対応)` | 旧レガシーソリューション (`mp.solutions`) から完全脱却 |
| **AI学習モデル** | `hand_landmarker.task` | Google公式の学習済みバイナリモデル（ローカル配置） |
| **通信プロトコル** | `UDP (User Datagram Protocol)` | IP: `127.0.0.1` / Port: `50001` (JSONパッキング) |

### インタプリタの設定
- **問題**: 
  - BeatSaber-LikeGameをルートとして開いていると、正しく`venv`のpython(`python.exe`)がインタプリタとして認識されないときがある。
  - mediapipeなどのライブラリがインポートできない。という問題が生じる
- **解決策**
  インタプリタの選択から直接ファイル(`venv/Scripts/python.exe`)を選択する必要がある。

なお、unityから動かす場合は問題なく動くので気にしなくてよい。
あくまで開発時にpythonだけで動作確認をする際の注意である。

---

## 3. マイルストーン・要件定義

### 🎯 次回ターゲット：タイトル画面の実装と本編連携
1. **TitleSceneのUI配置**: TextMeshProを用いたタイトルロゴ、STARTボタンのレイアウト。
2. **シーン遷移ロジック**: `SceneManager.LoadScene` を用いた `GameScene` へのスムーズな切り替え。
3. **UI操作のエクスペリエンス向上**: 最終目標として、マウスだけでなく「手首座標（`isRight` 判定）」による空中ハンドカーソルでのUI操作・選択の共通化。

### 🎸 最終マイルストーン（仮締切：19日目標）
- [ ] トラッキング実装 意図通りに動くように調整
- [ ] 音楽のテンポ（BPM）に同期した、奥から手前に流れるノーツ生成システムの実装。
- [ ] 剣（3Dモデルオブジェクト）とノーツのコライダー衝突（`OnTriggerEnter`）による判定・スコア加算システム。
- [ ] フルで1曲を途切れることなく遊べ、BGM終了とともにリザルト（結果表示）へ移行するゲームループの完遂。
- [ ] スタンドアロンアプリとしてのビルド検証（Windows用の `.exe` / Mac用の `.app`）。

---

## 4. 技術的負債と今後の検討事項 (バックログ)

- **ビルド時のスタンドアロン化**: 最終リリース時、`python-tracking` フォルダ（`venv` および `hand_landmarker.task` 含む）をUnityの `StreamingAssets` フォルダ配下に内包し、ビルド後のゲーム単体で独立起動できるようにC#のパス指定ロジックを最適化する。

---

## 問題点(Issue)と解決

### 1. ハンドトラッキングの動きがOS・カメラに依存して異なる問題
- **原因**: Pythonのデータ処理において、カメラから読み込んだ映像のピクセル解像度（縦横の長さ）に依存して座標スケーリングを行っていたため、WindowsとMacでカメラ仕様が異なるとズレが生じた。
- **解決策**: 手首の位置トラッキングにはカメラ解像度に依存しない「正規化座標（0.0 〜 1.0）」を用いる設計に変更。Unity側でその値を受け取り、プレイ画面のサイズに合わせてスクリーン座標でトラッキング（復元）させるアーキテクチャに修正した。

### 2. UDP通信の受信ストップとデータ形式エラー
- **症状**: Unity側で `udpClient.Receive` がブロッキング状態になり、データ受信が完全にストップした。
- **原因**: Python側でのデータ送信時、デバッグ作業の過程で `send_to_unity(data_dict)` の引数を誤って生のnumpy配列（`middle_v`）にしてしまっていた。そのため、`Object of type ndarray is not JSON serializable` のエラーが発生し、Python側の送信処理が落ちていた。
- **解決策**: 送信する変数を正しい辞書型（`data_dict`）に修正し、各numpy配列には `.tolist()` を適用してJSONシリアライズ可能な標準リストに変換することで開通。

### 3. 回転トラッキング（剣の傾き）のねじれ問題
- **症状**: UnityのHandAnchorの軸設定やLookRotationの使い方は正しいはずなのに、剣が明後日の方向を向いたり、回転がねじれたりした。
- **調査**: Python側でベクトルを視覚化（数値表示）し、自分の手の動きと符号（プラスマイナス）の変動をリアルタイムで監視した。
- **原因**: 
  1. **World座標の仕様の誤解**: `hand_world_landmarks` の原点は「画面の角」ではなく「手のひらの幾何学的な中心」にあった。
  2. **二重反転と外積の罠**: 画面原点に合わせようと計算の序盤で軸を反転させた結果、外積（クロス積）を計算した際にX軸やZ軸まで意図せず裏返る「数学的な歪み」が発生していた。
- **解決策**: ベクトル計算（相対ベクトルの取得や外積）はすべてMediaPipeの生座標（Y軸下向きプラス）のまま純粋に計算しきり、Unityに送信する直前の最後の最後で、Y軸のみを反転（`-1`を掛ける）してUnityの空間ルール（Y軸上向きプラス）に合わせる構成に変更した。

### 4. Z軸（奥行き）トラッキングのスケール問題
- **症状**: MediaPipeのZ軸データはスケールが非常に小さく、カメラからの絶対距離への依存も強いため、Unity空間で「前方に大きく切り込む」ような物理的な動きを再現するのが困難だった。
- **試行錯誤**: `5.0f + (handData.WristPos.z * 10^6)` のように倍率をかけて動的なZ軸を実装。`5 * 10^6` などにすると大きく動くが、直感的な操作感とはズレが生じた。
- **結論**: 現状の精度では違和感が残るため、完全なZ軸トラッキングはいったん妥協。「微小な動的変化（10^6倍）」を残して見た目を向上させるに留める。

### 5. ゲームコンセプトの戦略的ピボット（方針変更）
- **課題**: 現在の通信ラグやZ軸のトラッキング精度では、「剣を素早く振ってノーツをスパスパ切る（BeatSaber型）」というアクションの爽快感を得るのが難しいと判断。
- **新コンセプト**: **『剣の位置と角度をリズムに合わせて調整し、ノーツの指定にピッタリ揃えて迎撃する（リズムポーズ型）』**ゲームへと方針を転換。
- **展望**: これによりZ軸の激しい動きやミリ秒の遅延がゲーム性に悪影響を与えにくくなる。両手で多数のノーツの角度を合わせていくパズル的・ポーズ的な忙しさを生み出すことで、現在の技術制約の中でも十分に面白い「1曲」を完成させることが可能になる。

### 6. キャプチャ画面のアスペクト比によるゲーム画面とのギャップ
- **症状**: Windows環境などでカメラのキャプチャ画面が縦長の場合、正規化座標（0〜1）をそのままUnityの横長画面に適用すると、位置の移動量にギャップ（画面外に飛び出す等）を感じる。
- **解決策**: pythonでカメラ起動時に、解像度を指定して起動するようにした。
`cap.setでCAP_PROP_FRAME_HEIGHTや_WIDTH`を書き替えた。